This folder contains the previous root App Router pages/layout moved aside during the Studio4 → root migration.
You can restore any file by moving it back to the top-level 'app/' directory if needed.
